# macOS requirements for Violent-Python Showcase
#
# Used by the GUI's single-click Install when macOS is selected on the Setup page.
# You can edit this file via "Customize Requirements" before clicking Install.
#
# This file lists Python packages and suggests OS-level tools to improve the
# experience when running scripts from the GUI.
#
# OS packages (install via Homebrew):
#   # iTerm2 was previously suggested; current preferred terminals are kitty/wezterm/alacritty
# Optional terminals:
#   brew install kitty wezterm alacritty
#
# Python packages (installed with pip):
# Pillow is optional; enables animated GIF splash and image handling
pillow
# PrettyTable used by assignments/05_pil_search_images.py
prettytable
